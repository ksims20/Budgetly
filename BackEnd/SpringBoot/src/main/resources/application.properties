# application.properties - configures DB and server settings

# MySQL DB connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/budgetly_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=k7774363

# JPA (Hibernate) settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
